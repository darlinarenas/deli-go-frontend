(()=>{const API=String(window.BHUZ_API_URL||window.DELI_API_URL||window.API_BASE_URL||'https://deligo-backend-i554.onrender.com').replace(/\/+$/,'');let deferred=null;
function b64(s){const pad='='.repeat((4-s.length%4)%4),base=(s+pad).replace(/-/g,'+').replace(/_/g,'/'),raw=atob(base);return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))}
async function sw(){if(!('serviceWorker'in navigator))throw Error('Este navegador no admite instalación.');return navigator.serviceWorker.register('/service-worker.js')}
async function subscribe(meta={}){if(!('Notification'in window)||!('PushManager'in window))throw Error('Las notificaciones no están disponibles en este navegador.');const permission=await Notification.requestPermission();if(permission!=='granted')throw Error('Permiso de notificaciones no concedido.');const reg=await sw();const cfg=await fetch(API+'/api/tracking/config').then(r=>r.json());if(!cfg.vapidPublicKey)throw Error('Falta configurar VAPID_PUBLIC_KEY en Render.');let sub=await reg.pushManager.getSubscription();if(!sub)sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:b64(cfg.vapidPublicKey)});const r=await fetch(API+'/api/tracking/subscriptions',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify({...meta,subscription:sub.toJSON(),deviceName:navigator.userAgent.slice(0,180)})});if(!r.ok)throw Error((await r.json().catch(()=>({}))).message||'No se pudo activar.');localStorage.setItem('bhuz_push_enabled','1');return true}
function installBox(){if(document.getElementById('bhuzInstallBox'))return;const el=document.createElement('aside');el.id='bhuzInstallBox';el.className='bhuz-install-box';el.innerHTML='<button class="bhuz-install-close" aria-label="Cerrar">×</button><div><strong>Instala BHUZ</strong><small>Seguimiento y avisos de tus pedidos desde la pantalla de inicio.</small></div><button class="bhuz-install-action">Instalar</button>';document.body.appendChild(el);el.querySelector('.bhuz-install-close').onclick=()=>{el.remove();sessionStorage.setItem('bhuz_install_dismissed','1')};el.querySelector('.bhuz-install-action').onclick=async()=>{if(deferred){deferred.prompt();await deferred.userChoice;deferred=null;el.remove()}else{alert('En iPhone: abre Compartir y selecciona “Agregar a pantalla de inicio”.')}}}
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferred=e;if(!sessionStorage.getItem('bhuz_install_dismissed'))installBox()});
document.addEventListener('DOMContentLoaded',()=>{sw().catch(()=>{});const standalone=matchMedia('(display-mode: standalone)').matches||navigator.standalone;if(!standalone&&!sessionStorage.getItem('bhuz_install_dismissed')&&/iphone|ipad|ipod/i.test(navigator.userAgent))setTimeout(installBox,1800)});
window.BHUZ_PWA={subscribe,register:sw,install:()=>{installBox()}}})();
